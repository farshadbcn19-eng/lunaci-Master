name: Create ES - Shipping, Returns, Terms, Privacy (guarded)

on:
  push:
    branches:
      - claude/lunaci-barcelona-audit-r9mp7s
    paths:
      - '.github/workflows/create-es-simple-pages.yml'

jobs:
  create:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Install sshpass
        run: sudo apt-get install -y sshpass

      - name: Transfer script to server
        env:
          SSHPASS: ${{ secrets.HOSTINGER_SSH_PASS }}
          SSH_PORT: ${{ secrets.HOSTINGER_SSH_PORT }}
          SSH_USER: ${{ secrets.HOSTINGER_SSH_USER }}
          SSH_HOST: ${{ secrets.HOSTINGER_SSH_HOST }}
        run: |
          sshpass -e scp -o StrictHostKeyChecking=no -P "$SSH_PORT" \
            assets/translations/create-es-simple-pages.php "$SSH_USER@$SSH_HOST:/tmp/create-es-simple-pages.php"

      - name: Run guarded creation script
        env:
          SSH_PASS: ${{ secrets.HOSTINGER_SSH_PASS }}
          SSH_PORT: ${{ secrets.HOSTINGER_SSH_PORT }}
          SSH_USER: ${{ secrets.HOSTINGER_SSH_USER }}
          SSH_HOST: ${{ secrets.HOSTINGER_SSH_HOST }}
        run: |
          sshpass -p "$SSH_PASS" ssh -o StrictHostKeyChecking=no \
            -o ConnectTimeout=15 -o ServerAliveInterval=5 -o ServerAliveCountMax=3 \
            -p "$SSH_PORT" \
            "$SSH_USER@$SSH_HOST" \
            'WP_PATH="";
             for candidate in "$HOME/public_html" "$HOME" "$(pwd)"; do
               if [ -f "$candidate/wp-config.php" ]; then WP_PATH="$candidate"; break; fi;
             done;
             if [ -z "$WP_PATH" ]; then
               FOUND=$(find -L "$HOME" -maxdepth 8 -name "wp-config.php" -not -path "*/wp-content/*" -not -path "*/node_modules/*" 2>/dev/null | head -1);
               if [ -n "$FOUND" ]; then WP_PATH=$(dirname "$FOUND"); fi;
             fi;
             if [ -z "$WP_PATH" ]; then echo "ERROR: could not locate wp-config.php"; exit 1; fi;
             php -l /tmp/create-es-simple-pages.php;
             LINT_STATUS=$?;
             if [ "$LINT_STATUS" -ne 0 ]; then echo "ABORT: php -l failed"; exit 1; fi;
             wp eval-file /tmp/create-es-simple-pages.php --path="$WP_PATH";
             EVAL_STATUS=$?;
             rm -f /tmp/create-es-simple-pages.php;
             if [ "$EVAL_STATUS" -ne 0 ]; then echo "FAIL: wp eval-file failed (exit code $EVAL_STATUS)"; exit 1; fi;
             echo "OK: workflow completed"'
