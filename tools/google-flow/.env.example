# Copy to .env for local testing only. .env is git-ignored — never commit
# real values. In production this repo uses GitHub Actions secrets instead.
GOOGLE_FLOW_API_KEY=
GOOGLE_FLOW_API_BASE_URL=
GOOGLE_FLOW_GENERATE_ENDPOINT=/generate
